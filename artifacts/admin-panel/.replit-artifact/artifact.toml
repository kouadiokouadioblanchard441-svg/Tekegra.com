kind = "web"
previewPath = "/admin-panel/"
title = "Bot Admin Panel"
description = "Admin dashboard for the Lucky Jet AI Bot — a Telegram bot for Lucky Jet/Mines/Rocket Queen game signals. Provides login authentication, dashboard statistics, user management (approve/reject/ban), bot settings configuration, and broadcast messaging to all users."
version = "1.0.0"
id = "artifacts/admin-panel"
router = "path"

[[services]]
name = "web"
paths = [ "/admin-panel/" ]
localPort = 20130

[services.development]
run = "pnpm --filter @workspace/admin-panel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/admin-panel", "run", "build" ]
serve = "static"
publicDir = "artifacts/admin-panel/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20130"
BASE_PATH = "/admin-panel/"
